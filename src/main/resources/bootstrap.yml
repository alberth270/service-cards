spring:
  application:
    name: cards-service
  
  cloud:
    config:
      uri: http://localhost:8080
      
management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 9004
number-card: 111122223333444